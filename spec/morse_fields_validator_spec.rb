require 'spec_helper'

describe MorseFieldsValidator do
  it 'has a version number' do
    expect(MorseFieldsValidator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
